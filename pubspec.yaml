name: flutter_learn
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

# 第三方包引用
dependencies:
  flutter:
    sdk: flutter
  fluttertoast: ^3.0.3
  intl: ^0.15.7
  dio: ^2.1.0

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
